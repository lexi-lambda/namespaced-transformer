#lang info

(define collection 'multi)

(define deps
  '("namespaced-transformer-doc"
    "namespaced-transformer-lib"))
(define build-deps
  '())

(define implies
  '("namespaced-transformer-doc"
    "namespaced-transformer-lib"))
